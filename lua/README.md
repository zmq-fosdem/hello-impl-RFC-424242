# Reference RFC-424242 Implementation

## Dependencies

1. Lua
2. ZeroMQ
3. lzmq

### Install on OSX

Run `make install_osx`

## Run

One dependencies are installed: `make start` to start the server, and run `make run_client` to run the client.
